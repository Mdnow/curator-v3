# Журнал сессий — Curator v3

Короткий лог: что сделано за каждую сессию. Технические детали (как именно) — в `TECHNICAL_JOURNAL.md`.

Формат записи: дата → что сделано → итог/что проверено.

---

## Сессия: 2026-08-11 (деплой import на прод, закрытие VPN-хвостов)

**Тема:** сеть вернулась — закрыты хвосты из VPN_ЖУРНАЛ.md: деплой `/api/notes/import`, live-проверка целей.

**Что сделано:**
- **Railway-авторизация**: CLI был залогинен в аккаунт Marina DNLCHK (tiktok-watcher), а curator-v3 живёт в mdnow (`691553@bk.ru`). `railway logout` + логин через браузер → аккаунт mdnow, `railway link -p curator-v3` → production.
- **Деплой `railway up --detach`** — SUCCESS, сервер поднялся (uvicorn, `Application startup complete`).
- **Проверено живьём на проде**:
  - `GET /api/health` → 200.
  - `POST /api/login` (mdnow) → токен получен.
  - `POST /api/notes/import` → создал тестовую заметку id=364, затем `DELETE /api/notes/364` → 200 (мусор убран).
  - `GET /api/goals` → 4 активные + 6 архивных целей с цитатами.
- Закоммичен незакоммиченный `backend/routes/dreams.py` (убраны `::text` из SQL-параметров) — `c11d667`.

**Проверено:**
- Деплой в логах: цели реально генерировались на проде (`POST /api/goals/generate` 200).
- `test_all.py` запущен, но **завис на AI-запросах** (90с × модели = до 42 мин, см. TECHNICAL_JOURNAL §18.3) — процесс убит, не поломка. Оставлено в VPN_ЖУРНАЛ как долгая задача без спешки.

**Открыто:**
- Переключить tiktok-watcher с fallback на `/api/notes/import`.
- Интеграционные тесты — прогнать в фоне.

---

## Сессия: 2026-08-11 (кнопка проверки verify + починка реальных багов)

**Тема:** по запросу пользователя сделана «кнопка-проверка» на рабочем столе — один клик гоняет все проверки куратора, чтобы не верить агенту на слово.

**Что сделано:**
- **Починены 2 реальных бага**, найденные ruff (правило F821 — неопределённые имена):
  1. `backend/routes/chat.py:25` — `HTTPException` использовался без импорта (падал бы при «диалог не найден»).
  2. `backend/routes/notes.py:238` — `get_pool()` без импорта в `import_note._embed` (падал бы фоновый эмбеддинг импортированной заметки).
- Убраны неиспользуемые импорты (F401): `BaseModel` из main.py, `StreamingResponse/json/asyncio` из chat.py, `HTTPException` из dreams.py, `json` из favorites.py и test_feat.py, битая f-строка в test_all.py.
- **`ruff.toml`** для проекта: только критичные правила `F`+`E9` (реальные баги и синтаксис), стилистика (I001/UP045/BLE001/S110) намеренно отключена — это legacy проекта.
- **`verify.bat`** — скрипт проверки: py_compile всех backend-файлов + ruff + node --check app.js. Ярлык «Проверка куратора» на рабочем столе OneDrive.

**Проверено:**
- `ruff check` → 0 ошибок (было 2 F821 + F401/F541).
- `py_compile` всех файлов → 0 ошибок.
- `node --check frontend/js/app.js` → 0 ошибок.
- `verify.bat` целиком → «ВСЁ ЧИСТО».
- Коммит `9da5ce6`.

**Открыто:**
- Три проверки — офлайн, не трогают сеть/БД/AI. Интеграционные тесты (`test_feat.py`) по-прежнему ждут стабильной сети (см. VPN_ЖУРНАЛ.md).

---

## Сессия: 2026-08-06 (первая)

**Тема:** диагностика и починка сломанных категорий и куратора, сессии диалогов, паттерны, переанализ, журналы.

**Что сделано:**
- Найдены 2 причины «мёртвого» AI: невалидный `OPENROUTER_API_KEY` на Railway + узкая ротация free-моделей. Ключ обновлён, ротация расширена до 6 моделей с JSON-валидацией (`call_ai_json`).
- Архив переделан в нормальные **диалоговые сессии**: `session_id`, список диалогов (☰), «новый диалог» (✚), загрузка диалога в окно чата, удаление (✕), поиск, «очистить всё».
- Куратору добавлен контекст **паттернов и связей**: сводка тем, последние сны, избранное.
- Кнопка **«переанализ заметок»** (endpoint `POST /api/notes/reanalyze` + кнопка в сайдбаре и drawer) — исправляет старые заметки «без категории» со сломанного периода.
- Исправлена **двойная кодировка** в `index.html` (статический текст на проде показывал кракозябры, включая title).
- CORS сжат с `*` до прода + localhost. `.railwayignore`/`.gitignore` расширены до `*.png`.
- Создан `TECHNICAL_JOURNAL.md`, зафиксирована рекомендация freellmpool.
- Запущен фоновый переанализ 176 заметок Марины (user `mdnow`).

**Проверено:**
- Сессии UI на проде в браузере: отправка → ответ, список диалогов, возобновление, новый диалог, удаление.
- Переанализ полным циклом локально (тестовая заметка получила категорию), затем на проде через кнопку.
- Целостность данных: 222/222 заметок, 16/16 задач, 1/1 снов расшифровываются, потерь нет.
- Деплой `68cd8d0b SUCCESS`, UI после деплоя на чистом русском.
- Кодировка: title браузера «КУРАТОР v3» вместо кракозябр.

**Открыто/в процессе:**
- Фоновый переанализ 176 заметок идёт (~30с на заметку, завершится через ~1.5ч).

---

## Сессия: 2026-08-06 (QA-прогон + лимит OpenRouter)

**Тема:** код-ревью фронта (XSS), live-API-тесты, найден корень повторного сбоя AI — дневной лимит free-моделей OpenRouter.

**Что сделано:**
- Код-ревью frontend `app.js`: весь пользовательский контент (заметки, чат, сессии, инсайты) рендерится через `esc()` — XSS-векторов не найдено.
- Прогнан live-API-тест (ASGI, без сервера): auth, CRUD заметок/задач/снов, избранное, чат, сессии, поиск, бэкап, health, IDOR (попытки юзера B трогать данные юзера A — все блокируются), граничные случаи.
- **Найдена причина повторного «AI недоступен»**: OpenRouter free-тариф = **50 запросов/день на ключ**. Фоновый переанализ (125/176 заметок) + тесты сожгли лимит → все 6 free-моделей вернули 429. `X-RateLimit-Reset` = 00:00 UTC → восстановление через ~12ч, либо **+$10 → 1000 запросов/день** (рекомендуется).
- Фоновый переанализ **остановлен** (PID 32), чтобы не жечь лимит дальше. До 00:00 UTC AI не работает — это ожидаемо, не поломка.
- Исправлены 3 бага:
  1. **Переанализ записывал мусор**: при сбое AI условие `ai.get("category")` ловило фолбэк «без категории» как успех и перезаписывал заметки пустыми summary. Теперь пропускает при `ai.get("error")`. (То же в `_analyze_in_background` при создании заметки.)
  2. **Пагинация**: `page=0`/`limit=0` давали `OFFSET must not be negative` → 500. Теперь clamp `page>=1, 1<=limit<=100` (notes, chat/history).
  3. **Нечитаемые ошибки AI**: добавлен `AI_LAST_ERROR`; при лимите чат отвечает понятно: «Бесплатный лимит OpenRouter на сегодня исчерпан (50/день, сброс 00:00 UTC). Добавь $10 → 1000 запросов/день».
- Деплой `4967baee` поднят (Uvicorn running), прод онлайн.

**Проверено:**
- `page=0/-3`, `limit=0/-1/999999` → 200; `page=abc` → 422.
- `analyze_note` при лимите возвращает понятную ошибку.
- QA-тест до фикса: 40+ проверок прошли (включая все IDOR-попытки — изоляция юзеров работает); крах был только на `page=0` (исправлен).

**Открыто/рекомендации:**
- ~~Добавить $10 на openrouter.ai~~ — **отменено пользователем**: работаем только бесплатно (07.08). Если упрёмся в 50/день — расширять `FREE_MODELS` из каталога, затем `freellmpool`.
- ~~После сброса нажать «переанализ»~~ — **отменено пользователем**: не структурируем всё подряд с учётом лимитов.
- Тестовые юзеры в прод-БД — **почищены 07.08** (см. ниже).

---

## Сессия: 2026-08-07 (лимит сброшен, три долга закрыты, направление продукта)

**Тема:** проверка проду после сброса лимита, деплой незакоммиченных фиксов, чистка тестовых юзеров, закрытие трёх долгов QA-сессии, решение по «Задачам».

**Что сделано:**
- **AI на проде работает**: дневной лимит сброшен (00:00 UTC), 4 из 6 free-моделей отвечают 200. Обе gemma в 429 — это временный upstream-rate-limit (retry 24с), не суточный лимит.
- **Деплой** `ffe5a42f` SUCCESS: закоммичены фиксы QA-сессии (`51bb480` — AI_LAST_ERROR, пагинация, guard переанализа) + новые фичи (`3145f93`).
- **Тестовые юзеры удалены** (4 шт: `marina_test_pg`, `migrationtest`, `test`, `qa_val`) — в прод-БД только `mdnow`.
- **Три долга закрыты**: бэкап с AI-полями (§11.3), экранирование %/_ в поиске чата (§11.4), валидация пустых значений + лимиты длины (§11.5). Детали — в TECHNICAL_JOURNAL.md.
- **Продукт-решение**: раздел «Задачи» не используется (давит). Вместо него — слой **«проявленные цели»**: куратор как когнитивное зеркало выводит цели из суммарных заметок, без чекбоксов/дат/статусов. Референс — механики Mem AI (Discovery Engine, Heads Up, Actionable Chat). Одобрены варианты А (цели с цитатами-источниками) и Б (цель-диалог). Не внедрено — проектируется.

**Проверено:**
- Live: `/api/health` 200, модели 4×200 / 2×429.
- ASGI-тест на проде: валидация (пустые/длинные → 422, валидные → 200), поиск (`50%` → 1 результат, `50` → 2), бэкап с AI-полями.
- Деплой `ffe5a42f` SUCCESS, прод онлайн.

**Открыто/в процессе:**
- Проектирование слоя «проявленные цели» (замена «Задач»).
- Долги на потом: `freellmpool` (если упрёмся в лимиты), чекпоинты прогресса переанализа в UI, `chat_search` wildcard уже закрыт.

---

## Сессия: 2026-08-07 (Heads Up внедрён — семантический поиск)

**Тема:** реализация Heads Up из плана «проявленные цели»: pgvector-эмбеддинги заметок + блок «куратор видит связь» при вводе.

**Что сделано:**
- **Эмбеддинги**: `backend/ai.py` → `embed_text()` через бесплатную модель `nvidia/nemotron-3-embed-1b:free` (dim 2048, проверена ранее), плюс `cosine_similarity` (не понадобился — используем `pgvector <->`).
- **БД**: `db.py` → таблица `note_embeddings` (note_id PK, user_id, embedding vector(2048)) + `CREATE EXTENSION vector`. Таблица создана на проде вручную (миграция на лету).
- **Бэкфилл**: скриптом расшифрованы и заэмбеддичены **все 243 заметки** `mdnow` (пакетами по 40, дозапускаемый; лимит free 50/день позволил уложиться за несколько запусков).
- **Endpoint**: `POST /api/notes/related` — принимает черновик, возвращает топ-3 похожих заметок (`embedding <->`, с decrypt и ai_summary).
- **Фронт**: при вводе в поле заметки (≥12 символов, debounce 1.4с) блок **«куратор видит связь с…»** — дата + сниппет похожих заметок; клик отправляет запрос куратору с контекстом связанной заметки. CSS в `notes.css` (`.heads-up`).
- Заодно закоммичены ранее незакоммиченные: **goals-слой** (`goals.py`, таблица `goals`, `GOALS_SPEC.md`, `goals.css`, генерация целей из 100 последних заметок) и **AUTOSAVE-удаление** (chat.py, app.js).
- Коммит `1bdfdf9`, деплой SUCCESS, прод health 200.

**Проверено:**
- Семантический тест: запрос «Дубай 2027, 120к/мес» → топ-1 заметка о заработке $4k/мес, далее заметки о внутренних барьерах (релевантно).
- Эмбеддинги: 243/243 заметок, 0 ошибок.
- `python -c import backend.main` — OK; `node --check app.js` — OK.
- `/api/notes/related` на проде: 403 без токена (роут жив, защита работает).

**Открыто/в процессе:**
- Goals-слой внедрён полностью (backend + UI-раздел «Цели» с кнопкой «обновить цели» и кликом по цитате → день). Осталось проверить вживую реальную генерацию целей AI (не жегли лимит 50/день) — детали в TECHNICAL_JOURNAL §12.
- П.3 (thread_name) и П.5 (Actionable Chat) — в `FUTURE_TASKS.md` P2.

---

## Сессия: 2026-08-07 (Карта дня — когнитивная карта мышления)

**Тема:** по запросу «в чём я варюсь в каждом дне» реализован раздел «Карта» — пузырь дня: темы, настроение, фразы, нити. Без новых AI-запросов, AI-фраза по кнопке.

**Что сделано:**
- **Backend**: таблица `day_essences` (UNIQUE user_id+date, upsert); `DAY_ESSENCE_PROMPT` + `day_essence()` в ai.py; `GET /api/insights/day-map?date=` (агрегация категорий с sentiment, топ-фразы, нити дня, essence) и `POST /api/insights/day-map/{date}/essence` (фраза по кнопке, фоном, guard при ошибке — в БД не пишется).
- **Frontend**: пункт «Карта» (sidebar + mobile bar, глиф ◎). Пузыри тем (размер = объём, цвет = настрой), блоки «фразы дня» и «нити дня», клик по пузырю → заметки категории, кнопка «фраза дня». `daymap.css`.
- Заодно внесены в журналы: §12 (goals) в TECHNICAL_JOURNAL и правка статуса goals в SESSION_JOURNAL.

**Проверено:**
- ASGI-тест на реальной БД: агрегация (3 заметки → Саморазвитие 2×0.65, Работа 1, sent 0.37, «свобода»×2, 2 нити), пустой день, essence из таблицы, 401 без токена, чистка тестового юзера.
- Живой фоновый AI-прогон при лимите: все модели 429 → понятная ошибка «50/день, сброс 00:00 UTC», мусор не записан.
- Мок `day_essence`, `node --check app.js`.

**Открыто/в процессе:**
- «Путь целей» (следующий запрос): траектория из проявленных целей по времени + связи через общие темы. Решение: сначала бесплатная цепочка из готовых целей, AI-этапы — потом.

---

## Сессия: 2026-08-07 (интеграция с tiktok-watcher: `/api/notes/import` + PURPOSE.md)

**Тема:** внешний вход в зеркало — импорт готовых заметок из tiktok-watcher («Зеркало дня») без повторного AI-анализа; добавлен PURPOSE.md.

**Что сделано:**
- **`POST /api/notes/import`** (`ImportNoteReq`): content, note_date, tags, mood + готовые `ai_summary/ai_category/ai_sentiment/ai_keyphrases`. Создаёт заметку с AI-разметкой **без** `_analyze_in_background` — не жечь лимит OpenRouter. Единственный фоновый шаг — embedding для related-поиска. Коммит `59399a7`, запушен в GitHub.
- **`PURPOSE.md`** — первый в проекте: зачем создан куратор, поток, функции, факты, правила.

**Важно (грабли):**
- На прод куратора эндпоинт **НЕ задеплоен**: пуш в GitHub не триггерит Railway (см. TECHNICAL_JOURNAL §3.1). tiktok-watcher временно ходит на обычный `POST /api/notes` (fallback при 404/405).
- Логин `mdnow` + пароль проверен: `/api/login` → 200, токен получен.

**Проверено:**
- Импорт-эндпоинт зарегистрирован в коде (openapi локально), `ImportNoteReq` валиден.
- Логин в зеркало с Railway-переменными — работает (200).

**Открыто/в процессе:**
- [ ] Задеплоить куратора на Railway (`691553@bk.ru`) — чтобы `/api/notes/import` поехал и tiktok-watcher вернулся на него (уберём fallback).

---

## Сессия: 2026-08-08 (AI-провайдер → OpenCode Zen, уход от лимита OpenRouter)

**Тема:** по запросу «как использовать бесплатные модели opencode для куратора» переведён AI-слой с OpenRouter на OpenCode Zen. Выяснилось, что big-pickle — это Zen-модель.

**Что сделано:**
- **Найдено**: big-pickle и ещё 7 моделей — бесплатные модели **OpenCode Zen** (opencode.ai/zen). Эндпоинт OpenAI-совместимый: `https://opencode.ai/zen/v1/chat/completions`. У пользователя уже был доступ (пользуется big-pickle в opencode).
- **Ключ**: `ZEN_API_KEY` получен на opencode.ai/auth, записан в `.env` (в gitignore, в деплой не уходит).
- **Рефакторинг** `backend/ai.py` → провайдер-слой `PROVIDERS`: **Zen первым** (big-pickle, deepseek-v4-flash-free, nemotron-3-ultra-free, mimo-v2.5-free + 4 запасных), **OpenRouter fallback** (`OPENROUTER_MODELS`). Все вызывающие (`analyze_note`, `analyze_dream`, `dream_insight`, `daily_patterns`, `thread_suggest`, `generate_goals`, `day_essence`, `chat_with_context`) не менялись.
- **Таймауты 15с→90с** — big-pickle/deepseek это reasoning-модели, думают дольше 15с (замерено ~11с на простой JSON). **Лимиты токенов подняты** (thread_suggest 200→1000, goals 1200→2000, day_essence 300→800 и др.) — reasoning-токены считаются в `max_tokens`.
- Сообщения про лимиты нейтрализованы (убраны упоминания OpenRouter/50-в-день).
- `backend/config.py`: `ZEN_URL`, `ZEN_API_KEY`.

**Проверено:**
- Прямой вызов Zen: `big-pickle` и `deepseek-v4-flash-free` возвращают 200, отдают чистый русский JSON в `content` (reasoning в отдельном поле — игнорируется).
- `call_ai_json` через рефакторинг: `providers = ['zen','openrouter']`, заметка про Дубай → корректный JSON от big-pickle.
- `py_compile` всех тронутых файлов — OK.

**Не проверено / открыто:**
- Полный серверный смоук-тест **не прошёл**: Neon рвёт соединение на ~26с (`ConnectionDoesNotExistError: connection was closed in the middle of operation`) — сеть/VPN пользователя нестабильна, не код. Проверить `python test_feat.py` когда сеть стабилизируется.
- **Приватность**: big-pickle в бесплатный период использует данные запросов для улучшения модели (см. доки Zen). Куратор шлёт личные мысли/сны открытым текстом — решено: для личного использования ок, но держать в уме.
- Free-модели Zen «на ограниченное время» — для того оставлен fallback на OpenRouter.
- На прод (Railway) ничего не задеплоено — только локальные правки.

---

## Сессия: 2026-08-09 (деплой Zen на прод, CORS-чистка, два Railway-аккаунта)

**Тема:** перенос Zen-провайдера из локалки на прод, убрана путаница с доменами, попытка смоук-теста (снова сеть).

**Что сделано:**
- **Выяснено про Railway**: curator-v3 живёт в аккаунте **mdnow** (e-mail `691553@bk.ru`, вход через GitHub), tiktok-watcher — в **Marina DNLCHK** (dnlchk.mn@gmail.com, вход через Google). В папке `curator-v3` CLI был привязан к tiktok-watcher (`railway status` показывал чужой проект, `railway project list` — только текущий аккаунт). Перепривязка: `railway link -p curator-v3` → production → сервис curator-v3.
- **Деплой Zen на прод**: `railway variables --set "ZEN_API_KEY=..."` на проекте curator-v3 (production); `railway up --detach` → деплой `9e89ede5` **SUCCESS**, лог: `Application startup complete`, uvicorn на 8080.
- **CORS-чистка**: из `backend/main.py` и `PURPOSE.md` убран устаревший домен `curator-v3-production-c830.up.railway.app`; остался актуальный `curator-v3-production.up.railway.app` (RAILWAY_PUBLIC_DOMAIN). Деплой `f2f88392` **SUCCESS**.
- **Чистка доков от «50/день»**: `PURPOSE.md` (домен, провайдер Zen + OpenRouter fallback, лимит), `GOALS_SPEC.md` §8, `FUTURE_TASKS.md` (freellmpool — теперь запас на случай упора Zen+OpenRouter).
- **Поиск дублей/ошибок**: дублей функций нет (91 `def`), дублей эндпоинтов нет (45 роутов, пути уникальны). `grep c830` по проекту — 0 совпадений.
- Записан в память (mem0) факт про два Railway-аккаунта и их логины.

**Проверено:**
- Оба деплоя SUCCESS, прод-логи: сервис стартует без ошибок на новом коде.
- `railway variable list` — ZEN_API_KEY на проде, все нужные переменные на месте (DATABASE_URL/Neon, OPENROUTER_API_KEY, CURATOR_SECRET, CURATOR_ENCRYPTION_KEY).
- Домен: в проекте только `curator-v3-production.up.railway.app` (grep 0 совпадений на c830).

**Открыто/в процессе:**
- **`python test_feat.py` снова не прошёл** — та же причина: Neon рвёт соединение на данных (`ConnectionDoesNotExistError: connection was closed in the middle of operation` / WinError 1225). Диагностика: TCP до Neon (порт 5432) проходит (`TcpTestSucceeded=True`), но asyncpg 5/5 попыток `SELECT 1` → WinError 1225. Сеть/VPN пользователя нестабильна, это не код. Прогнать тест при стабильной сети.

---

## Сессия: 2026-08-09 (тезисы и саммари у заметок)

**Тема:** по запросу «в куратор сделать саммари и чёткие тезисы» добавлен слой тезисов; заодно полный транскрипт + заголовок в tiktok-watcher.

**Что сделано:**
- **Колонка `ai_theses`** у заметок (миграция в `init_db`, `backend/db.py`).
- **Генерация тезисов**: в `ANALYZE_NOTE_PROMPT` добавлено поле `theses` (2-4 чётких тезиса); `analyze_note` возвращает и сохраняет их. Работает и для обычных заметок, и для импортированных.
- **Импорт из tiktok-watcher**: `/api/notes/import` принимает `ai_theses`; watcher теперь шлёт суть как `ai_summary` (а не сырой срез `content[:400]`) и тезисы как `ai_theses`.
- **UI**: блок тезисов в карточке заметки (`note-ai-theses`), CSS в `notes.css`.
- **Бэкап**: `ai_theses` включено в экспорт.
- Обновлены `FUTURE_TASKS.md` (архив), журналы.

**Проверено:**
- `py_compile` всех тронутых файлов — OK.
- Импорты backend-модулей — OK.
- ruff: только предсуществующие BLE001/S110 (стиль проекта), новых ошибок нет.
- Интеграционные тесты (`test_feat.py`) — не запускались: пишут в прод-BD Neon и жгут AI-лимит; сетевая нестабильность из прошлых сессий.

**Открыто:**
- Live-проверка на проде: заметка из tiktok с тезисами в зеркале (после деплоя обоих приложений).
- Переанализ старых заметок, чтобы они тоже получили тезисы (кнопка «переанализ» уже есть).

---

## Сессия: 2026-08-10 (процесс: ADR-реестр, правила, деплой; попытка живого прогона целей)

**Тема:** внедрение Agentic Engineering Workflow: реестр решений, карта документов, правила для агента; коммит и деплой последних изменений; диагностика генерации целей.

**Что сделано:**
- Создан **реестр решений** `docs/adr/`: INDEX.md + ADR-0001..0006 (провайдер Zen, шифрование, JWT, Goals-слой, Railway, тезисы).
- **Карта документов** в корневом `README.md`: 4 слоя (Зачем / Куда идём / Решения / Дневник) — кто за что отвечает.
- **AGENTS.md** (воркспейс): рабочий цикл (SPEC до кода, коммит после шага, Verifier, ADR, code-review), границы автономии, протокол Blocking.
- Чистка доков от «50 запросов/день» (README, PURPOSE, GOALS_SPEC, FUTURE_TASKS, AGENTS) — актуально только Zen + OpenRouter fallback.
- **Скиллы**: 87 лишних перемещены в `_skills-archive`, осталось 5 (`eunoia-marina`, `eunoia-autonomous`, `obsidian-mem`, `memai-connector`, `tdd`).
- Коммит `fbb69cc` (Zen `ai.py`, PURPOSE, TECH_MAP, docs/adr) — точка отката.
- Деплой `4ec22294` **SUCCESS** (06:23) — прод на последнем коде.
- Диагностика AI: Zen отвечает (синтетическая генерация целей — цель с 2 цитатами за ~3с).

**Проверено:**
- Деплой SUCCESS, прод-переменные на месте (ZEN/OPENROUTER/DATABASE/CURATOR_*).
- TCP до Neon (порт 5432) проходит; расшифровка 100 заметок `mdnow` работает.
- ruff/py_compile — правки были только в доках и `.md`, код не менялся.

**Открыто / не получилось:**
- **Живой прогон генерации целей на реальных данных `mdnow` не завершён**: локально Neon рвёт соединение на реальных запросах (WinError 1225, §16.6 журнала), на проде не достучаться — VPN/сеть отвалилась (`curl prod = 000`). Закрыть: прод → раздел «Цели» → «обновить цели».
- Push на GitHub не делался (не запрашивалось).

**После обеда (обновление зависимостей, Dependabot):**
- GitHub Dependabot: 17 уязвимостей на default branch (1 critical, 7 high). Причины: устаревшие пины в `requirements.txt`.
- Обновлены версии: fastapi 0.115.0→0.141.1, uvicorn 0.30.0→0.52.1, pydantic 2.9.0→2.13.4, asyncpg 0.29.0→0.31.0, cryptography 43.0.0→50.0.0, httpx 0.27.0→0.28.1, python-multipart 0.0.9→0.0.32.
- **`python-jose` заменён на PyJWT 2.13.0** (критические CVE-2024-33663/33664 — JWT alg-confusion); правка `auth.py` (2 импорта + except). Убран неиспользуемый `passlib`.
- Проверка в чистом venv: `import backend.main` OK, JWT roundtrip OK, py_compile OK. Ruff: только предсуществующие замечания в других файлах.
